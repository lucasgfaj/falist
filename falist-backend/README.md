# Falist


Scaffold para backend FastAPI + PostgreSQL.


## Como rodar (com Docker)


1. Copie/cole os arquivos do scaffold em uma pasta `falist/`.
2. Ajuste `DATABASE_URL` no docker-compose se quiser.
3. Rode: `docker-compose up --build`
4. A API estará em `http://localhost:8000` e docs em `http://localhost:8000/docs`.